# 因子体检（factor_checkup）指标口径说明

对应脚本 `prepare_factor_checkup.py`，输出 `factor_checkup.json`，页面 `factor_checkup.html`。
最后一次核对：2026-09-16。

---

## 0. 两条独立的数据线

| | 因子侧 x | 目标侧 y |
|---|---|---|
| 输入 | factor_library 的 303 条日频序列 | 曲线/期货主力价格 |
| 变换 | **3 种形态**（原值 / MA10→60日百分位 / 5日差分） | 无形态，只有"未来 N 日累计变动" |
| 滞后 | **LAG = 1 天**（机构行为 T+1 发布） | 不滞后，y_t 就是 t→t+N |

**关键：形态只作用于因子，不作用于目标。** 目标侧唯一的选择是周期 N ∈ {1, 5, 10, 20}。

### 0.1 三种形态（都在因子上算）

| 形态 | 公式 | 损失天数 | 含义 |
|---|---|---|---|
| raw 原值 | `x_t = X_t` | 0 | 水位 |
| pct 百分位 | `x_t = pct60(MA10(X)_t)` | 前 68 天为 NaN（MA10 前 9 天 + 百分位窗口前 59 天） | 相对自己近 3 个月的位置 |
| diff 5日差分 | `x_t = X_t − X_{t−5}` | 前 5 天 | 边际方向 |

- MA10 用 `ma_nan`：窗口内**非空比例 ≥ 70%** 才输出均值，个别缺失日不会让整段塌掉。
- 百分位用 `rolling_pct`：`(小于当前值的个数 + 0.5×等于的个数) / 窗口内非空个数 × 100`，只看**过去 60 日**，无前视。
- 三者都只用 t 及之前的信息；算完形态之后**再整体右移 1 天**作为滞后。

### 0.2 目标 y 的构造

| 目标 | level | y_t | 单位 |
|---|---|---|---|
| y10 | 10 年国债收益率 | `(Y_{t+N} − Y_t) × 100` | bp |
| s3010 | 30Y − 10Y | 同上 | bp |
| tl3t | `3·P_T − 1·P_TL` | `(level_{t+N} − level_t) / (3·P_T + P_TL)_t × 100` | % |

`3T−TL` 与"30Y−10Y 走阔"同向。分母用 t 时刻的本金，所以量纲是"组合的百分比收益"。

### 0.3 配对

逐日配对 `(x_{t−1}, y_t)`，只保留两者都非空的日期 → 有效样本 n。**n < 200 的组合直接不进表**（`MIN_OBS`）。

---

## 1. 为什么不能直接用普通 OLS 的 t

N=5 时，`y_t` 与 `y_{t+1}` 共用 4/5 的单日涨跌，残差强自相关。名义上有 n 个样本，真实独立信息只有约 n/N 段。
普通 OLS 标准误被系统性低估，**t 值虚高得离谱**（实测：N=20 时 NW-t 判据下 29 个"显著"，换成不依赖渐近假设的判据后剩 0 个）。

所以下面**三个判据并列**，各自解决同一个问题的不同侧面。

---

## 2. 相关性与稳定性

| 列 | 定义 |
|---|---|
| `ic` | **Spearman 秩相关** `rank(x)` 与 `rank(y)` 的 Pearson，全样本一个数 |
| `icp` | **Pearson 线性相关**（对照用，看是否被极端值主导） |
| `icir` | 滚动 **60 日 Pearson IC** 序列的 `mean / std(ddof=1)`，需序列长度 >10 |
| `h6` | 最近 125 个有效样本（约 6 个月）的 Spearman IC |
| `yr21…yr26` | 分年 Spearman IC，该年样本 ≥40 才算 |

> 实现细节（如实记录）：`ic` 列是 Spearman，但 `icir` 用的是滚动 **Pearson** IC 序列，两者口径不完全一致，读表时留意。

IC 的含义与股票多因子**完全不同**：这里没有截面，是把每个交易日当成一个观测，问"因子高低"与"随后 N 日涨跌"在时间轴上是否同步。

---

## 3. 三个 t 判据

### 3.1 `t` —— Newey-West 修正 t 值（功效最高，也最容易虚高）

1. OLS `y = a + b·x`，得 `b = Σ(x_c·y_c) / Σ(x_c²)`，`x_c = x − mean(x)`
2. 残差 `e = y − a − b·x`，令 `u_t = x_c,t · e_t`
3. 滞后阶 `L = max(N − 1, floor(4·(n/100)^(2/9)), 1)`
4. Bartlett 核：`V = Σu_t² + 2·Σ_{l=1..L} (1 − l/(L+1))·Σ_t u_t·u_{t−l}`
5. `se = √V / Σ(x_c²)`，`t = b / se`

把残差自相关计入方差。但 L 取到 N−1 甚至更高时，小样本下 V 会被低估，**N 越大虚高越严重**。

### 3.2 `ti` / `pind` —— 不重叠子样本 t（FDR 主判据）

1. 按步长 N 抽稀：`idx = 0, N, 2N, …`（在有效样本序号上等距取，不是日历日精确间隔）
2. 抽稀后样本数 m，要求 `m ≥ 15`
3. 普通 OLS：`s² = Σe²/(m−2)`，`se = √(s² / Σx_c²)`，`ti = b/se`
4. `pind = 2·P(T_{m−2} > |ti|)`

思路：每 N 天取一个点，相邻点的收益窗口几乎不重叠 → 样本近似独立 → 不依赖任何渐近假设。
**FDR 主判据用 `pind`。**

### 3.3 `p` —— 循环置换检验精确 p（最保守）

1. 取 `rx = rank(x)`，`ry = rank(y)`
2. 把 `ry` 做全部 n 个循环移位，用 **FFT 循环互相关**一次算完 n 个 `r_k`（O(n log n)，确定性、无随机种子、可复现）
3. `r_0` 即真实的 Spearman；`p = #{k : |r_k| ≥ |r_0|} / n`

零假设是"x 与 y 在任何时间错位上都没有关联"。循环移位**完整保留了 y 的自相关结构**，所以这是最贴合实际、也最保守的判据。
代价：如果 x 本身高度持久（如 MA10），几乎所有移位都给出高相关，p 会趋近 1，**功效很低**。
所以 `p` 显著是"强证据"，`p` 不显著**不等于**没用。

### 3.4 `sig` —— 最终显著性判定

1. 在**每个 (形态 × 目标 × 周期) 块内**（约 280 行）对 `pind` 做 **BH-FDR 5%**：排序后找最大 k 使 `p_(k) ≤ 0.05·k/m`，前 k 个判显著
2. 同样对 `p` 再做一次 FDR
3. 硬门槛 `|IC| ≥ 0.05`（防 IC≈0 却被极端值撑高的假 t 值）

| sig | 含义 |
|---|---|
| 0 | 不显著 |
| 1 | 独立样本 FDR 显著 |
| 2 | 独立样本与置换检验**都**显著（最可信） |

> 注意：FDR 是**块内**校正，不是 10063 次全局校正。若要最严格，可改成全局 FDR，门槛会明显更严。

**读表口诀**：`t` 显著但 `ti`/`p` 不显著 ⇒ 信号多半来自共同低频趋势，慎用。

---

## 4. 分组收益（时序版"分层"）

分组变量是**滚动 250 日 Z-score**（`rolling_z`，再滞后 1 日）。
用全样本分位是未来函数，**禁用**；滚动 Z 只用过去 250 天，无前视。

1. 五等分切点：`±0.8416`（20/80 分位）、`±0.2533`（40/60 分位）
2. `q1…q5` = 各组内 `y` 的均值（该组样本 ≥20 才算，否则 NaN），单位同目标（bp 或 %）
3. `qd = Q5 − Q1`：多空价差
4. `qt`：`Q5 组 vs Q1 组` 的均值差 NW-t（dummy 回归，x=1/0，滞后 `max(N−1, 1)`）
5. `mono`：相邻组差全负 = `−1`（单调递减），全正 = `+1`，否则 `0`

---

## 5. 信号化后的胜率与赔率

阈值 `SIGNAL_Z = 1.0`：z > 1 持多，z < −1 持空，其余空仓。阈值 `TAIL_Z = 1.5` 用于尾部赔率。

| 列 | 定义 |
|---|---|
| `win` | 有持仓的日子里 `pos·y > 0` 的比例（样本 ≥20） |
| `wl` | 多头日 `y > 0` 的比例 |
| `ws` | 空头日 `y < 0` 的比例 |
| `pl` | 盈亏比 = `mean(盈利单) / |mean(亏损单)|`，基于 `pos·y`，需盈亏各 ≥5 笔 |
| `tail` | 同上，但只取 `|z| > 1.5` 的信号 |
| `turn` | 持仓方向日均变化频率（换手频率） |

注意：这些**没有扣成本**。turn 高的信号必须自己折算手续费再判断。

---

## 6. 诊断与 regime

| 列 | 定义 |
|---|---|
| `kurt` | 因子形态的超额峰度（Fisher 定义，正态 = 0） |
| `acf` | 因子形态的一阶自相关 |
| `rgu` / `rgd` | 目标 **60 日趋势**上行 / 下行子样本的 IC（子样本 ≥40） |
| `rghv` / `rglv` | 目标 **20 日已实现波动率**高于 / 低于全样本中位数的子样本 IC |

regime 切分是**自动**的（60 日趋势方向 + 20 日波动率中位数），不是人工牛熊划分。

---

## 7. 参数速查

```
LAG=1  MIN_OBS=200  Z_WIN=250  ICIR_WIN=60  FDR_ALPHA=0.05
MIN_IC_STRONG=0.05  SIGNAL_Z=1.0  TAIL_Z=1.5  RECENT6=125
HORIZONS=[1,5,10,20]
```

## 8. 当前结论（2026-09-15 跑批）

- 10063 套检验，置换 p 值**整体完美均匀**：`P(p<0.05)=0.0509`、中位数 `0.481` ⇒ 303 条原始因子**没有系统性信号**
- 仅 7 个通过独立样本 FDR；最有逻辑的一个：`回购·货基·正回购余额`（pct 形态 / y10 / N=20），
  IC = −0.30、ti = −4.56、p置换 = 0.010、Q5−Q1 = −6.95bp、单调递减
- 增量方向：**复合因子**（机构分歧度、配置盘 vs 交易盘）+ 正交化，而不是继续堆原始序列
